# Build-a-Base-Smart-Contract-Deployment-Tool
A CLI tool to compile and deploy smart contracts on Base.

Deep Features to Include:

Hardhat integration

Multiple network support (Base mainnet/testnet)

Gas estimation & verification

Deployment history logs
